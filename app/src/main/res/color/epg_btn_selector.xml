<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 非触摸模式下获得焦点并单击时的字体颜色 -->
    <item android:state_pressed="true" android:state_focused="true" android:color="@color/epg_text_select" />
    <!-- 获得焦点时的字体颜色 -->
    <item android:state_focused="true" android:color="@color/epg_text_select" />
    <!-- 默认时的字体颜色 -->
    <item android:color="@color/epg_text_normal" />

</selector>