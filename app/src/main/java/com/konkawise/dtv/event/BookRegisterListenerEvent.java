package com.konkawise.dtv.event;

public class BookRegisterListenerEvent {
    public boolean isRegisterBookListener;

    public BookRegisterListenerEvent(boolean isRegisterBookListener) {
        this.isRegisterBookListener = isRegisterBookListener;
    }
}
